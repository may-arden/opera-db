<%= @error %>

<%= form_for([@song, @note]) do |f| %><br>
  note title 
  <%= f.text_field :title %><br>
  wrote your note here 
  <%= f.text_area :content %>
  <%= f.hidden_field :song_id %><br>
  <%= f.submit %>
<% end %>