<%= render partial: 'layouts/header', locals: {object: @user} %>

<h1>sign up</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.text_field :password_digest %>

  <%= f.submit "sign up" %>
<% end %>